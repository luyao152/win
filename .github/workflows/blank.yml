name: 构建x64+arm64并自动加版本号

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 解除权限限制
        run: |
          Set-ExecutionPolicy Unrestricted -Force -Scope LocalMachine
          Set-ExecutionPolicy Unrestricted -Force -Scope CurrentUser
          reg add HKLM\SYSTEM\CurrentControlSet\Control\FileSystem /v LongPathsEnabled /t REG_DWORD /d 1 /f

      # ========== x64 构建 ==========
      - name: 构建 x64
        run: |
          $zip = Get-ChildItem ./x64/*.zip | Select -First 1
          Expand-Archive $zip.FullName ./x64 -Force
          cd ./x64
          cmd /c uup_download_windows.cmd

          $iso = Get-ChildItem *.iso | Select -First 1
          if($iso) {
            $ver = $iso.Name -replace '.*(\d+\.\d+).*','$1'
            $newName = "Windows_x64_$ver.iso"
            Rename-Item $iso.FullName -NewName $newName -Force
          }

      - name: 上传 x64
        uses: actions/upload-artifact@v4
        with:
          name: Windows_x64
          path: x64/Windows_x64_*.iso

      # ========== arm64 构建 ==========
      - name: 构建 arm64
        run: |
          $zip = Get-ChildItem ./arm64/*.zip | Select -First 1
          Expand-Archive $zip.FullName ./arm64 -Force
          cd ./arm64
          cmd /c uup_download_windows.cmd

          $iso = Get-ChildItem *.iso | Select -First 1
          if($iso) {
            $ver = $iso.Name -replace '.*(\d+\.\d+).*','$1'
            $newName = "Windows_arm64_$ver.iso"
            Rename-Item $iso.FullName -NewName $newName -Force
          }

      - name: 上传 arm64
        uses: actions/upload-artifact@v4
        with:
          name: Windows_arm64
          path: arm64/Windows_arm64_*.iso
